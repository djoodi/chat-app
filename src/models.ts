export interface IAuth {
    username: string;
    password: string;
}

export interface IServer {
    id: string;
    title: string;
}

export interface IChannel {
    id: string;
    title: string;
}