export interface AuthInfo {
    username: string;
    password: string;
}

export interface ServerInfo {
    id: string;
    title: string;
}

export interface ChannelInfo {
    id: string;
    title: string;
}